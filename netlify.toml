[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing — send every URL to index.html so React Router handles it
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
